model UserPurchase {
  id            String          @id @default(cuid())
  userId        String
  eventId       String
  description   String
  amount        Int
  currency      String
  date          DateTime @default(now())
  @@schema("gitgrasp_schema")
}
